language: python
python:
  - "2.7"
  - "nightly" # currently points to 3.7-dev
# command to install dependencies
install: "pip install -r ./Source/Python/python_sort/requirements.txt"
# command to run tests
script: cd Source/Python/python_sort && paver test_all
